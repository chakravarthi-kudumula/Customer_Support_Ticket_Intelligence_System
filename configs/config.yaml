project:
  name: customer-support-ticket-intelligence
  random_seed: 42

paths:
  raw_data_dir: data/raw
  processed_data_dir: data/processed
  raw_complaints_csv: data/raw/complaints.csv
  processed_complaints_csv: data/processed/complaints_clean.csv
  models_dir: artifacts/models
  embeddings_dir: artifacts/embeddings
  vector_store_dir: artifacts/vector_store
  reports_dir: artifacts/reports

dataset:
  text_column: Consumer complaint narrative
  target_column: Product
  id_column: Complaint ID

training:
  test_size: 0.2
  validation_size: 0.1

